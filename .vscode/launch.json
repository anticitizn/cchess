{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Debug",
            "type": "cppdbg",
            "request": "launch",
            "program": "${workspaceFolder}/cchess",
            "stopAtEntry": false,
            "cwd": "${workspaceFolder}"
        }
    ]
}